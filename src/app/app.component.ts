import { Component } from '@angular/core';

@Component({
  // this is the tag that is embedded
  selector: 'app-root',
  // the html file that we are using
  templateUrl: './app.component.html',
  // this is the style that we are using
  styleUrls: ['./app.component.scss']
})
export class AppComponent {
  // adding a type 

}
